// домашка 7
// Задание 1
// Преобразовать строку 'js' в верхний регистр.
let str = 'js';
console.log(str.toUpperCase());


// Задание 2
// Создать функцию, которая принимает массив строк и строку. Функция должна вернуть новый массив, содержащий только те элементы первого массива, которые начинаются со второй строки. Регистр символов не влияет на результат.


// Критерии оценки
// Работа будет оценена по следующим критериям
// Функция принимает массив строк и строку, возвращает новый массив с элементами, начинающимися с заданной строки, регистр не влияет.
// Результат работы загружен на GitHub и отправлена ссылка на pull request.
// Подсказка
// Используйте функции 
// toLowerCase()
//  и 
// startsWith()
//  для сравнения начала строк.


// Задание 3
// Округлить число 32.58884:

// До меньшего целого.
// До большего целого.
// До ближайшего целого.

let num = 32.58884
console.log(Math.floor(num)); //меньшее целое
console.log(Math.ceil(num));  //большее целое
console.log(Math.round(num)); //ближайшее целое

// Задание 4
// Найти минимальное и максимальное значения из чисел 52, 53, 49, 77, 21, 32 и вывести их в консоль.
const numbs = [52, 53, 49, 77, 21, 32];
console.log(Math.min(...numbs));
console.log(Math.max(...numbs));

// Задание 5
// Создать функцию, которая выводит в консоль случайное число от 1 до 10.
function getRandomNumber() {
    return Math.floor((Math.random() * 10) + 1);
}
console.log(getRandomNumber());

// Задание 6
// Написать функцию, которая принимает целое число и возвращает массив случайных чисел от 0 до этого числа. Длина массива должна быть в два раза меньше переданного числа.


// Критерии оценки
// Работа будет оценена по следующим критериям
// Функция принимает целое число и возвращает массив случайных чисел от 0 до этого числа, длина массива — половина от переданного числа.
// Результат работы загружен на GitHub и отправлена ссылка на pull request.
// Подсказка
// В JavaScript функция 
// Math.random()
//  возвращает случайное число от 
// 0
//  (включительно) до 
// 1
//  (не включая).
function halfNumberArray() {
    let Number = +prompt('Введите целое число');
    let array = [];
    for (let i = 0; i  < Number / 2; i++) {
        array.push(Math.floor(Math.random() * Number));
    }
    return array;    
}
console.log(halfNumberArray(Number));

// function generateRandomArray(max) {
//     const array = [];
  
//     for (let i = 0; i < max / 2; i++) {
//       array.push(Math.floor(Math.random() * max));
//     }
  
//     return array;
//   }
  
//   console.log(generateRandomArray(10)); // Пример использования функции
// Задание 7
// Создать функцию, которая принимает два целых числа и возвращает случайное число в этом диапазоне.


// Критерии оценки
// Работа будет оценена по следующим критериям
// Функция принимает два целых числа и возвращает случайное число в этом диапазоне с использованием 
// Math.random(), Math.floor() или Math.ceil().

function generateRandomNumberInPeriod(mini, maxi) {
    return Math.floor(Math.random() * (maxi - mini + 1)) + mini;
}
console.log(generateRandomNumberInPeriod(5, 40));

// Задание 8
// Вывести в консоль текущую дату.


// Критерии оценки
// Работа будет оценена по следующим критериям
// Текущая дата выведена в консоль с использованием объекта 
// Date.

console.log(new Date());

// Задание 9
// Создать переменную 
// currentDate
// , хранящую текущую дату. Вывести дату, которая наступит через 73 дня после текущей.


// Критерии оценки
// Работа будет оценена по следующим критериям
// Переменная 
// currentDate создана, дата через 73 дня вычислена и выведена с использованием методов getDate() и setDate().

// let currentDate = new Date();
// currentDate.setDate(currentDate.getDate() + 73);

// let dateNow = new Date();
// console.log(dateNow.setDate(dateNow.getDate() + 73));
// // Создание переменной currentDate, хранящей текущую дату
// let currentDate = new Date();

// // Вывод текущей даты в консоль
console.log(currentDate);

// Функция для расчёта даты, которая наступит через 73 дня после текущей
function calculateFutureDate() {
  // Получение количества миллисекунд в 73 днях
  const daysInMilliseconds = 73 * 24 * 60 * 60 * 1000;

  // Добавление количества миллисекунд к текущей дате
  currentDate.setTime(currentDate.getTime() + daysInMilliseconds);

  // Вывод будущей даты в консоль
  console.log("Дата, которая наступит через 73 дня: " + currentDate);
}

calculateFutureDate();

// Задание 10
// Написать функцию, которая принимает дату и возвращает ее в формате:


// Дата: [число] [месяц на русском] [год] — это [день недели на русском].
// Время: [часы]:[минуты]:[секунды]

// Критерии оценки
// Работа будет оценена по следующим критериям
// Функция принимает дату и возвращает её в заданном формате: "Дата: [число] [месяц на русском] [год] — это [день недели на русском]. Время: [часы]:[минуты]:[секунды]".
// Результат работы загружен на GitHub и отправлена ссылка на pull request.
// Подсказка
// Дату и время можно получить из объекта Date.
function formatDate(date) {
    const days = ["Понедельник", "Вторник", "Среда", "Четверг", "Пятница", "Суббота", "Воскресенье"];
    const months = ["Январь", "Февраль", "Март", "Апрель", "Май", "Июнь", "Июль", "Август", "Сентябрь", "Октябрь", "Ноябрь", "Декабрь"];
    const year = date.getFullYear();
    const day = date.getDate();
    const month = date.getMonth();
    const weekDay = days[date.getDay()];
    const hours = date.getHours();
    const minutes = date.getMinutes();
    const seconds = date.getSeconds();
    return `Дата: ${day}.${month}.${year} - это ${weekDay}
    Время: ${hours}:${minutes}:${seconds}`;
}
console.log(formatDate(new Date()));